import { Component } from '@angular/core';

/** Placeholder for T041's routing shell — the real form/loading/error/success states are T043. */
@Component({
  selector: 'app-calculator',
  template: `<p>Calculator — coming soon.</p>`,
})
export class CalculatorComponent {}
