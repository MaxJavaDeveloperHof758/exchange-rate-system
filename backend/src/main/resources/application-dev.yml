# Local/reviewer profile only (quickstart.md). Schema creation is owned by
# Flyway (db/migration) for every profile, including this one — this file no
# longer needs its own ddl-auto override (it used to set "update" here to
# work around the base H2 file-datasource having no schema-creation default
# at all; PostgreSQL + Flyway made that problem, and this override, obsolete).
# Kept as a profile file for DevDataSeeder's @Profile("dev") activation and
# any future dev-only property.
